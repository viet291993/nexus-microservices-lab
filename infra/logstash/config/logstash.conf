input {
  tcp {
    port => 50000
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "nexus-logs-%{service_name:-unkown}-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
